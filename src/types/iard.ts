import type { mockPolicesIard, iardRepartition } from "@/data/mock/iard.mock";

export type PoliceIard = (typeof mockPolicesIard)[number];
export type IardRepartition = (typeof iardRepartition)[number];
